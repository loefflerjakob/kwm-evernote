import { TagFormErrorMessages } from './tag-form-error-messages';

describe('TagFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new TagFormErrorMessages()).toBeTruthy();
  });
});
